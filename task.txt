<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
</head>
<body>
    <h1>Student Information</h1>
    <button onclick="showResult()">Show Student Info</button>
    <script>
        var student = {
            name: "Ariful Islam",
            age: 20,
            subjects: ["Mathematics", "Physics", "Chemistry"],
            marks: [85, 90, 78],
            totalMarks: 0,
            isAdult: false,
            isPassed: true,
        };
        for(var i = 0; i < student.marks.length; i++) {
            student.totalMarks += student.marks[i];
        }
        student.isAdult = student.age >= 18;
        for(var i = 0; i < student.marks.length; i++)
        {
            if(student.marks[i] < 50) {
                student.isPassed = false;
                break;
            }
        }
        for(var i = 0; i < student.marks.length; i++)
        {

        }
        function showStudentInfo() {
            console.log("Name: " + student.name);
            console.log("Age: " + student.age);
            console.log("Result:");
            for(var i = 0; i < student.subjects.length; i++) {
                console.log(student.subjects[i] + ": " + student.marks[i]);
            }
        }

        function showResult() {
            alert("Name: " + student.name + "\n" +
                  "Total Marks: " + student.totalMarks + "\n" +
                "Is Adult: " + (student.isAdult ? "Yes" : "No") + "\n" +
                  "Result: " + (student.isPassed ? "Passed" : "Failed"));
        }
        console.log(showStudentInfo())

    </script>
</body>
</html>